import { Box } from '@mui/material';

function BoardBar() {
  return (
    <Box
      sx={{
        bgcolor: 'primary.dark',
        width: '100%',
        display: 'flex',
        alignItems: 'center',
      }}
    >
      BoardBar
    </Box>
  );
}

export default BoardBar;
