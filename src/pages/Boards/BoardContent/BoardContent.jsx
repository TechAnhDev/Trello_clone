import { Box } from '@mui/material';
function BoardContent() {
  return (
    <Box
      sx={{
        bgcolor: 'primary.main',
        width: '100%',
        display: 'flex',
        alignItems: 'center',
      }}
    >
      BoardContent
    </Box>
  );
}

export default BoardContent;
